# 프론트 개발 서버 포트
VITE_PORT=5173

# Vite dev proxy가 전달할 백엔드 주소 (같은 EC2에서 실행 시 localhost:3000)
VITE_API_PROXY_TARGET=http://localhost:3000

# API 직접 호출 URL (빌드/운영 시 설정)
# nginx 프록시 사용 시: /api
# 백엔드 직접 호출 시: http://<EC2-공인IP>:3000
# 미설정 시 /api (Vite proxy 또는 nginx /api 경로 사용)
VITE_API_URL=http://LB-Backend-277043075.ap-northeast-2.elb.amazonaws.com
